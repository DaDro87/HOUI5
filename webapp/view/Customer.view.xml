<mvc:View
    controllerName="at.clouddna.training02.zhoui5.controller.Customer"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:u="sap.uxap"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
>
    <u:ObjectPageLayout
        id="ObjectPageLayout"
        showTitleInHeaderContent="true"
        showFooter="{= ${viewModel>/editMode}}"
        upperCaseAnchorBar="false"
    >
        <u:headerTitle>
            <u:ObjectPageDynamicHeaderTitle>
                <u:expandedHeading>
                    <HBox>
                        <Title text="{i18n>customerDetails}" />
                        <ToolbarSpacer />
                    </HBox>
                </u:expandedHeading>
                <u:actions>
                    <Button id="app_edit_button" text="{i18n>edit}" type="Emphasized" icon="sap-icon://edit" 
                    press="onEditPressed" visible="{= !${viewModel>/editMode}}" />
				</u:actions>
            </u:ObjectPageDynamicHeaderTitle>
        </u:headerTitle>
        <u:footer>
            <Toolbar id="app_bar_footer">
                <ToolbarSpacer />
                <Button id="app_save_button" text="{i18n>save}" type="Accept" icon="sap-icon://save" 
                press="onSavePressed" visible="{= ${viewModel>/editMode}}" />
                <Button id="app_cancel_button" text="{i18n>cancel}" type="Reject" icon="sap-icon://cancel" 
                press="onCancelPressed" visible="{= ${viewModel>/editMode}}" />
            </Toolbar>
        </u:footer>
    </u:ObjectPageLayout>
</mvc:View>
